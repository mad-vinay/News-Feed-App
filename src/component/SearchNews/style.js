import styled from 'styled-components';

const Container = styled.label`
  position: absolute;
  top: 20px;
  right: 20px;
  input {
    padding: 5px 10px;
  }
`;

const Styled = {
  Container
};
export default Styled;
